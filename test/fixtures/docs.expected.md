# CLI Quickstart

Use the CLI to convert copied HTML to compact markdown context.

## Run Locally

1.  Install dependencies with Bun.
2.  Run the clean command with piped HTML.
3.  Paste the markdown output into your LLM chat.

```
bun run src/cli.ts clean -i snippet.html
```
